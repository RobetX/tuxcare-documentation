import{_ as e,n as a,p as n,a7 as s}from"./framework-0ba8e1ea.js";const t={},i=s(`<h1 id="fips-packages-for-almalinux" tabindex="-1"><a class="header-anchor" href="#fips-packages-for-almalinux" aria-hidden="true">#</a> FIPS packages for AlmaLinux</h1><p>Below are the instructions for installing the TuxCare FIPS 140-3 validated modules for AlmaLinux 9.2, they should be run as root.</p><p>By installing this software, you agree to be bound by the terms of the <a href="https://tuxcare.com/wp-content/uploads/2023/09/COMMUNITY-EULA.txt" target="_blank" rel="noopener noreferrer">TuxCare Community EULA</a>.</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># dnf -y install https://repo.tuxcare.com/fips/tuxcare-fips-release-latest-9.noarch.rpm
# dnf -y install openssl-3.0.7-20.el9_2.tuxcare.1 kernel-5.14.0-284.11.1.el9_2.tuxcare.5
# fips-mode-setup --enable
# reboot
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Once you&#39;ve logged in after the reboot, run this to confirm it worked (doesn&#39;t have to be root):</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$ fips-mode-setup --check
$ uname -r
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>You should get a message like this (may say <code>aarch64</code> instead of <code>x86_64</code>):</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>FIPS mode is enabled.
5.14.0-284.11.1.el9_2.tuxcare.5.x86_64
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>If you wish to stay on the FIPS validated kernel/openssl packages when a newer AlmaLinux package is available, you can use <code>dnf versionlock</code> like so:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># dnf -y install &#39;dnf-command(versionlock)&#39;
# dnf versionlock add openssl*tuxcare* kernel*tuxcare*
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>To revert to the previous behaviour of getting updated kernel/openssl packages from AlmaLinux, run the following as root:</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code># dnf versionlock delete openssl*tuxcare* kernel*tuxcare*
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,12),l=[i];function d(r,o){return a(),n("div",null,l)}const u=e(t,[["render",d],["__file","index.html.vue"]]);export{u as default};
